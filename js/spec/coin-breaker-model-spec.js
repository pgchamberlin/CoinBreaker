define([
    'js/src/models/coin-breaker-model'
], function(CoinBreakerModel) {
    describe('check the CoinBreakerModel behaves as it should', function() {
        it('should not pass this test', function() {
            expect(false).toBe(true);
        });
    });
});

